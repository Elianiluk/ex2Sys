#define numArray 10
void selectItems(float weights[], float values[], char *strings[], int numStrings);
int shortestPath(int [][numArray],int,int);
int isShortestPath(int [][numArray],int,int);
void getNumbers(int mat[][numArray]);
